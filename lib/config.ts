// Single switch for the app call-to-action.
// Leave empty ('') while the app has no public URL: all app buttons stay
// hidden and the site remains a pure content surface.
// When the Lovable app is live (e.g. at app.strofyt.com), set it here and
// "Open the app" / "Start free" buttons appear in the header and hero.
export const APP_URL = '';
